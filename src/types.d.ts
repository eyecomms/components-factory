import {CSS} from "@stitches/react/types/css-util";
import {ConfigType} from "@stitches/react/types/config";

export type CSSProps = {css?: CSS<ConfigType.Media, ConfigType.Theme, ConfigType.ThemeMap, ConfigType.Utils>}
